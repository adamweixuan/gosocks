# usage 

```shell
socks5 ツ ./bin/gosocks-darwin-amd64-v3 --help
Usage of ./bin/gosocks-darwin-amd64-v3:
  -ipver
    	enable ipv6.
  -memSize int
    	memory size limit.
  -port uint
    	socks 5 server listen port.  (default 10086)
  -verbos
    	verbose log.
```